import gsap from "gsap";
import React, { useRef } from "react";


export default function Zoom(starsArray) {
    const tl = useRef()

    let ctx = gsap.context(() => {
        starsArray.forEach(star => {
            
        })
    })
}